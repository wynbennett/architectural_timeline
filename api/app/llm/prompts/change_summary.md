Repository {owner}/{name}. Compare tag {prev_tag} (earlier) to tag {tag} (later).

<previous_graph tag="{prev_tag}">
{previous_graph}
</previous_graph>

<current_graph tag="{tag}">
{current_graph}
</current_graph>

<computed_diff>
{computed_diff}
</computed_diff>

Write a short narrative (3 to 6 sentences) of how the architecture changed between these two
tags: what was added, removed, split, merged, or rewired, and the likely reason where the
code makes it evident. Refer to components by name. If nothing structural changed, say so
and mention any notable module-level movement instead.
